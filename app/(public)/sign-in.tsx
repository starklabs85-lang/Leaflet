import { SignInContent } from "@/components/auth/SignInContent";

export default function SignInScreen() {
  return (
    <SignInContent
      eyebrow="Leaflet"
      title="Keep your plants alive."
      body="Sign in to save your plants, sync care reminders, and keep your collection available across devices."
    />
  );
}
